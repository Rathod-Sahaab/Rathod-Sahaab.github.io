import React from "react"

import "./404.scss"

const NotFoundPage = () => (
  <section id="not-found" className="page">
    <img src="./images/404.svg" />
    <h1>Not found</h1>
  </section>
)

export default NotFoundPage
